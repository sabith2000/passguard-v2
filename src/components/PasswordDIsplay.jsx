import React, { useState } from 'react';
import { Copy, Check } from 'lucide-react';
export function PasswordDisplay({ passwords }) { const [copied, setCopied] = useState(false); const handleCopy = () => { const textToCopy = passwords.join('\n'); navigator.clipboard.writeText(textToCopy).then(() => { setCopied(true); setTimeout(() => setCopied(false), 2000); }); }; return ( <div className="relative"> <textarea readOnly value={passwords.join('\n')} className="w-full h-40 bg-slate-900 text-white p-4 rounded-lg text-sm font-mono tracking-wider border-2 border-slate-700 focus:outline-none resize-none" placeholder="Your generated passwords will appear here..." /> <button onClick={handleCopy} title="Copy all passwords" className="absolute top-3 right-3 p-2 text-slate-400 hover:text-white transition-all duration-200 rounded-lg hover:bg-slate-700" > {copied ? <Check className="text-emerald-500" size={20} /> : <Copy size={20} />} </button> </div> );}