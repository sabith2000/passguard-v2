import React from 'react';
const strengthLevels = { 0: { text: "Very Weak", color: "bg-red-700" }, 1: { text: "Weak", color: "bg-red-500" }, 2: { text: "Moderate", color: "bg-yellow-500" }, 3: { text: "Strong", color: "bg-emerald-500" }, 4: { text: "Very Strong", color: "bg-green-500" } };
export function StrengthMeter({ options }) { const calculateStrength = () => { let score = 0; if (options.length >= 8) score++; if (options.length >= 12) score++; if (options.length >= 16) score++; let charTypes = 0; if (options.includeUppercase) charTypes++; if (options.includeLowercase) charTypes++; if (options.includeNumbers) charTypes++; if (options.includeSymbols) charTypes++; if (options.customChars) charTypes++; if (charTypes >= 3) score++; return Math.min(4, Math.max(0, score)); }; const score = calculateStrength(); const level = strengthLevels[score]; return ( <div className="space-y-2"> <div className="w-full bg-slate-700 rounded-full h-2.5"> <div className={`h-2.5 rounded-full transition-all duration-300 ${level.color}`} style={{ width: `${(score + 1) * 20}%` }} ></div> </div> <p className={`text-right font-medium text-sm transition-colors ${level.color.replace('bg-','text-')}`}> {level.text} </p> </div> );}
